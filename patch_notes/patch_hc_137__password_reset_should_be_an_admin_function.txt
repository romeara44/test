INSERT INTO  `hipaacar_app`.`mail_templates` (
`mt_id` ,
`mt_name` ,
`mt_key` ,
`mt_subject` ,
`mt_text` ,
`mt_active` ,
`mt_create_date`
)
VALUES (
NULL ,  'Forgot Password Request',  'forgot_password_request',  'Forgot Password Request',  'User <User Name> with email <User Email> forgot password.',  '1', 
CURRENT_TIMESTAMP
);

INSERT INTO  `hipaacar_app`.`mail_templates` (
`mt_id` ,
`mt_name` ,
`mt_key` ,
`mt_subject` ,
`mt_text` ,
`mt_active` ,
`mt_create_date`
)
VALUES (
NULL ,  'Reset Password',  'reset_password',  'Reset Password',  '<div style="font-size: 24px; color: #189696"><Full Name>,</div>
<br />
<b>Your password was changed by <Admin Name>.</b>
<br /><br />

Your login information is:<br />
Login page: http://hipaa.carosh.com/<br />
Your login: <login><br/>
Your password: <password><br />
<br />',  '1', 
CURRENT_TIMESTAMP
);

ALTER TABLE `users` ADD COLUMN `u_forgot_password` TINYINT(1) DEFAULT '0';